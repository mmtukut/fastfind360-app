'use client';

// This component is currently not in use as markers are rendered directly in MapView.
// It can be used in the future if we decide to use react-map-gl layers with Google Maps.

export function BuildingLayer() {
  return null;
}
